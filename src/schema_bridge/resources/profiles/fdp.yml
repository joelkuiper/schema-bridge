name: fdp
description: FDP-compatible RDF output (metadata service + catalog + datasets)
fetch:
  graphql: graphql/resources.graphql
  root_key: Resources
export:
  construct: fdp_construct.sparql
  outputs:
    - jsonld
    - ttl
mapping:
  entity_name: Resource
  subject_path: resource
  id_field: id
